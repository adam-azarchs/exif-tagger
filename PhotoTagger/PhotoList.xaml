<UserControl x:Name="photoList" x:Class="PhotoTagger.PhotoList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:PhotoTagger"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="100">
    <ListBox ItemsSource="{Binding Photos, ElementName=photoList}"
             SelectionMode="Extended" Name="ListBox" SelectionChanged="onSelectionChanged">
        <ListBox.ItemTemplate>
            <DataTemplate DataType="{x:Type local:Photo}">
                <local:PhotoListItem Photo="{Binding Mode=OneWay}" />
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>
